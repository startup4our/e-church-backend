services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: echurch_app
    ports:
      - "8000:80" # mapeia porta 80 do container para 8000 do host
    volumes:
      - .:/var/www/html  # mantém volume para desenvolvimento
    networks:
      - laravel

networks:
  laravel:
    driver: bridge
